import 'package:flutter/material.dart';

class BardenBooks extends StatelessWidget {
  const BardenBooks({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}