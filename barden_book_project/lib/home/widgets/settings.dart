import 'package:flutter/material.dart';

class BardenSettings extends StatelessWidget {
  const BardenSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("settings");
  }
}