import 'package:flutter/material.dart';

class BardenBookTable extends StatelessWidget {
  const BardenBookTable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}